#include<stdio.h>
int power(int,int);
main()
{
    int i;
    for (i=0;i<10;++i)
        printf("-2 in %d-th grage is %d\n",i,power(-2,i));
}
int power (a,b)
{
    int i,m;
    m=1;
    for (i=0;i<b;++i) m=m*a;
    return(m);
}
